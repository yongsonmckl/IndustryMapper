{
  "name": "industrymapper-root",
  "private": true,
  "scripts": {
    "build": "node scripts/vercel-build.mjs",
    "dev": "npm --prefix web run dev",
    "lint": "npm --prefix web run lint"
  }
}
